export * from './get-token';

export function createProduct(params: {
  productName: string;
  productImage: string;
  sellingPrice: string;
  status: string;
}) {
  throw new Error('Function not implemented.');
}
